
import { makeStyles } from "@material-ui/core/styles";
import { colors } from "../../assets/colors";

export const useStyles = makeStyles(() => ({
    container: {
        backgroundColor: colors.YELLOW_MELI,
        height: 60
    }
}));



